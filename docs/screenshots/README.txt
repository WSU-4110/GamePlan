screenshots checklist (amjad)

- take these screenshots and save them here:
  1) npm-test-pass.png  -> terminal showing `npm test` with all tests passing
  2) branch.png         -> terminal showing `git branch` with `amjad_unittest` active

notes
- keep filenames lowercase as above
- no other artifacts (like coverage) are required unless your instructor asks
